# Daily Report 2026-08-02 (Cycle 2)

## Objective
Phase 0 lanjutan: hapus sisa agency studio copy yang kontradiksi dengan Vision, dan fix broken navigation link yang menyebabkan 404.

## What Changed (files + reason)

### 1. `src/pages/About.tsx`
- **Bug fix:** Hero link "See experiments" → `/experiments` (route tidak ada) → **404**. Dikoreksi ke `/work`.
- **Root cause:** Commit `b10cb39` fix nav di SiteLayout tapi ketinggalan link di About hero.

### 2. `src/components/SiteLayout.tsx`
- Footer masih bilang "A digital product studio for teams that need sharper interfaces and cleaner launches."
- Kontradiksi langsung dengan H1 hero: "Not a portfolio. Not a studio."
- Rewritten: "An autonomous AI engineering laboratory — research, build, document, experiment."
- CTA "Start a project" → "Read the vision" (konsisten dengan identitas lab, bukan agency sales).

### 3. `src/pages/Contact.tsx`
- Strip semua sisa agency copy:
  - "Website redesigns for product or service companies" → "Autonomous engineering experiments to run alongside Hermes"
  - "project brief" framing → "engineering note" framing
  - "design and implementation thinking" → "agent-driven platform engineering"
- Fix orphan link: `/services` (tidak ada di nav, route orphan) → `/about`.
- Checklist konten diubah dari sales-oriented ke engineering-oriented.

## Risk
- **Low.** Semua perubahan adalah copy text dan route path. Tidak ada logic change, tidak ada dependency baru.
- **Reversibility:** Setiap patch adalah single-line atau small-block replacement, mudah di-revert per-file.
- **Remaining gap:** `Services.tsx` page masih ada dan tidak di-nav (orphan route). Bisa di-remove di cycle berikutnya atau di-rename jadi "Capabilities" page yang lebih Vision-aligned.

## Validation
- `npm run build` ✓ — 1650 modules transformed, 4.97s, 0 errors
- `npm run lint` ✓ — clean, 0 warnings
- Bundle: `index-BAQpD0Im.js` 219.66 kB (gzip 67.84 kB) — tidak ada regression

## Lessons Learned
1. **Refactor besar (Phase 0 reframe) sering meninggalkan jejak.** Audit regex setelah content rewrite besar wajib — "studio", "portfolio", "redesign", "/services" harus di-grep semuanya, bukan hanya halaman utama.
2. **Navigation consistency bug invisible kalau cuma lihat nav bar.** Link di dalam page hero (PageHero `links` prop) adalah source of truth kedua untuk routing. Keduanya harus sinkron dengan `App.tsx` route definitions.
3. **Footer adalah tempat agency copy paling bandel** — jarang di-review setelah hero di-reframe, padahal user lihat setiap halaman.

## Next Priority
Phase 0 tersisa:
1. **Remove dead dependencies** — `framer-motion`, `zustand` (0 import), `useTheme` hook (0 caller). Ini `npm install` berat yang tidak terpakai.
2. **Services.tsx orphan route** — `App.tsx` masih mount `/services` tapi tidak ada di nav. Decision: remove page, atau rebrand jadi "Capabilities" yang Vision-aligned.
3. **Static generation (vite-ssg)** — Google masih lihat empty `<div id="root">`. Phase 1 priority.
